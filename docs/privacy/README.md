# Informativa privacy (Play)

Pagina pubblica: `index.html`.

URL previsto (GitHub Pages su branch `main`, cartella `/docs`):

`https://renatizzi.github.io/BoxManagerNew/privacy/`

In GitHub → Settings → Pages: Source = Deploy from a branch, Branch = `main`, Folder = `/docs`.

Lo stesso URL è in `PrivacyPolicy.PUBLIC_URL` e va inserito nel campo Privacy policy della Play Console.
